import keyMirror from "keymirror";

const actionTypes = {
  FETCH_IDENTITY_IMG: null,
  FETCH_IDENTITY_IMG_SUCCESS: null,
  FETCH_IDENTITY_IMG_FAILURE: null,
};

export default keyMirror(actionTypes);
